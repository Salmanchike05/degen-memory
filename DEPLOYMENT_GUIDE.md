# 🚀 Руководство по деплою Degen Memory в Base App

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка GitHub репозитория

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Название: `degen-memory` (или любое другое)
   - Выберите **Public** или **Private**
   - НЕ добавляйте README, .gitignore, лицензию (у нас уже есть)

2. **Инициализируйте Git и загрузите код:**
   ```bash
   cd /Users/airm1/Documents/degen-memory
   git init
   git add .
   git commit -m "Initial commit: Degen Memory game for Base App"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/degen-memory.git
   git push -u origin main
   ```
   Замените `YOUR_USERNAME` на ваш GitHub username.

### Шаг 2: Деплой на Vercel

1. **Подключите репозиторий к Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub
   - Нажмите "Add New Project"
   - Выберите репозиторий `degen-memory`
   - Vercel автоматически определит Next.js

2. **Настройте переменные окружения:**
   - В настройках проекта добавьте:
     ```
     NEXT_PUBLIC_URL=https://your-project.vercel.app
     ```
   - Или оставьте пустым - Vercel автоматически установит URL после деплоя

3. **Отключите Deployment Protection:**
   - Settings → Deployment Protection
   - Отключите "Vercel Authentication"
   - Это нужно для Base App preview tool

4. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения (обычно 2-3 минуты)
   - Скопируйте URL проекта (например: `degen-memory.vercel.app`)

### Шаг 3: Обновление конфигурации

1. **Обновите `minikit.config.ts` с реальным URL:**
   - После деплоя Vercel автоматически установит `VERCEL_PROJECT_PRODUCTION_URL`
   - Или установите переменную `NEXT_PUBLIC_URL` в Vercel

2. **Обновите `src/lib/wagmi.ts`:**
   ```typescript
   appLogoUrl: "https://your-project.vercel.app/icon.png"
   ```

3. **Закоммитьте и запушьте изменения:**
   ```bash
   git add .
   git commit -m "Update URLs for production"
   git push
   ```
   Vercel автоматически передеплоит проект.

### Шаг 4: Создание accountAssociation

1. **Перейдите на Base Build:**
   - Откройте https://www.base.dev/preview?tab=account

2. **Введите URL приложения:**
   - Вставьте ваш Vercel URL (например: `degen-memory.vercel.app`)
   - Нажмите "Submit"

3. **Подпишите manifest:**
   - Нажмите "Verify"
   - Подпишите транзакцию своим кошельком
   - Скопируйте `accountAssociation` объект:
     ```json
     {
       "header": "...",
       "payload": "...",
       "signature": "..."
     }
     ```

4. **Обновите `minikit.config.ts`:**
   - Вставьте скопированные значения в `accountAssociation`
   - Закоммитьте и запушьте:
     ```bash
     git add minikit.config.ts
     git commit -m "Add accountAssociation"
     git push
     ```

### Шаг 5: Проверка через Base Build Preview

1. **Проверьте manifest:**
   - Перейдите на https://www.base.dev/preview
   - Введите ваш URL
   - Проверьте вкладки:
     - **Embeds** - должно показывать превью
     - **Account association** - должно быть "Verified"
     - **Metadata** - все поля должны быть заполнены

2. **Проверьте запуск приложения:**
   - Нажмите кнопку "Launch" в preview
   - Убедитесь, что игра запускается корректно

### Шаг 6: Публикация в Base App

1. **Создайте пост в Base App:**
   - Откройте Base App
   - Создайте новый пост
   - Вставьте URL вашего приложения: `https://your-project.vercel.app`
   - Добавьте описание (опционально)

2. **Опубликуйте:**
   - После публикации Base App автоматически проиндексирует ваше приложение
   - Оно появится в поиске и категориях через несколько минут

## ✅ Чеклист перед деплоем:

- [ ] Код загружен на GitHub
- [ ] Репозиторий подключен к Vercel
- [ ] Деплой на Vercel успешен
- [ ] Deployment Protection отключен
- [ ] `NEXT_PUBLIC_URL` установлен (или используется автоматический)
- [ ] `appLogoUrl` обновлен в `wagmi.ts`
- [ ] `accountAssociation` создан через Base Build
- [ ] Manifest проверен через Base Build Preview
- [ ] Приложение запускается корректно
- [ ] Пост создан в Base App

## 🔧 Полезные команды:

```bash
# Проверка локально
npm run dev

# Сборка для проверки
npm run build

# Проверка линтера
npm run lint

# Git команды
git status
git add .
git commit -m "Your message"
git push
```

## 📝 Важные замечания:

1. **URL должен быть HTTPS** - Vercel автоматически предоставляет SSL
2. **Все изображения должны быть доступны** - проверьте что они загружены в `public/`
3. **Manifest должен быть доступен** - проверьте `https://your-domain.com/.well-known/farcaster.json`
4. **Webhook endpoint** - должен отвечать на POST запросы (уже настроен)

## 🆘 Если что-то не работает:

1. **Проверьте логи Vercel:**
   - Deployment → Logs

2. **Проверьте Base Build Preview:**
   - https://www.base.dev/preview
   - Вкладка "Metadata" покажет все ошибки

3. **Проверьте manifest:**
   - Откройте `https://your-domain.com/.well-known/farcaster.json`
   - Должен вернуть валидный JSON

## 🎉 После успешного деплоя:

Ваше приложение будет:
- ✅ Доступно по URL
- ✅ Видно в Base App после публикации поста
- ✅ Индексируется в поиске Base App
- ✅ Отображается в категории "games"

Удачи с деплоем! 🚀
