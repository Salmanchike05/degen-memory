# 🔄 КРИТИЧЕСКИ ВАЖНО: Перезапуск сервера

## ❗ Проблема:

После восстановления `globals.css` и `postcss.config.mjs` нужно **ОБЯЗАТЕЛЬНО** перезапустить dev сервер, иначе CSS не пересоберется!

## 🚀 Шаги для исправления:

### 1. Остановите текущий dev сервер

В терминале, где запущен `npm run dev`:
- Нажмите `Ctrl + C` (или `Cmd + C` на Mac)
- Дождитесь полной остановки

### 2. Очистите кэш Next.js

```bash
cd /Users/airm1/Documents/degen-memory
rm -rf .next
```

### 3. Запустите сервер заново

```bash
npm run dev
```

### 4. Дождитесь полного запуска

Вы должны увидеть:
```
▲ Next.js 14.2.35
- Local:        http://localhost:3000
✓ Ready in X.Xs
```

### 5. Обновите страницу в браузере

**ВАЖНО:** Сделайте жесткую перезагрузку (очистка кэша):
- **Windows/Linux:** `Ctrl + Shift + R`
- **Mac:** `Cmd + Shift + R`

Или:
1. Откройте DevTools (F12)
2. Правой кнопкой на кнопку обновления
3. Выберите "Очистить кэш и жесткая перезагрузка"

## ✅ После перезапуска должно быть:

- ✅ Черный фон
- ✅ Белый текст
- ✅ Кнопки с правильными стилями (серые, скругленные)
- ✅ Фиолетовый цвет для "Degen"
- ✅ Кнопки уровней вертикально

## 🔍 Если все еще не работает:

1. Проверьте консоль браузера (F12) → вкладка **Console**
2. Проверьте вкладку **Network** → найдите `layout.css`
3. Убедитесь, что файл загружается (статус 200, не 404)

**Выполните эти шаги - это обязательно для применения стилей!**
